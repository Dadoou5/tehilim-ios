package com.david.tehilim.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Palette Tehilim — mirror des tokens iOS (bleu eau accent, fond bleu très clair).
 * Material 3 utilise ces couleurs via ColorScheme dans Theme.kt.
 */

// Accent principal — bleu eau (iOS accentMain)
val AccentMain = Color(0xFF1E6091)
val AccentMuted = Color(0xFFA9CCE3)

// Fonds
val BgPrimaryLight = Color(0xFFF4F8FB)   // bleu très clair — l'eau
val BgSurfaceLight = Color(0xFFFFFFFF)
val BgElevatedLight = Color(0xFFEFF6FA)

val BgPrimaryDark = Color(0xFF0E1620)
val BgSurfaceDark = Color(0xFF1A2330)
val BgElevatedDark = Color(0xFF26313F)

// Texte
val TextPrimaryLight = Color(0xFF0F1F2E)
val TextSecondaryLight = Color(0xFF5C6B7A)
val TextTertiaryLight = Color(0xFF8A99A7)

val TextPrimaryDark = Color(0xFFE6EEF5)
val TextSecondaryDark = Color(0xFFA8B6C4)
val TextTertiaryDark = Color(0xFF6C7C8A)

// Divider + error
val DividerToken = Color(0xFFD8E1E9)
val DividerDark = Color(0xFF2D3845)
val ErrorToken = Color(0xFFC0392B)
