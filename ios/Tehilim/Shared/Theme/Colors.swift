import SwiftUI

extension Color {
    static let bgPrimary     = Color("bgPrimary",     bundle: .main)
    static let bgSurface     = Color("bgSurface",     bundle: .main)
    static let bgElevated    = Color("bgElevated",    bundle: .main)
    static let textPrimary   = Color("textPrimary",   bundle: .main)
    static let textSecondary = Color("textSecondary", bundle: .main)
    static let textTertiary  = Color("textTertiary",  bundle: .main)
    static let accentMain    = Color("accentMain",    bundle: .main)
    static let accentMuted   = Color("accentMuted",   bundle: .main)
    static let dividerToken  = Color("divider",       bundle: .main)
    static let errorToken    = Color("errorToken",    bundle: .main)
}
